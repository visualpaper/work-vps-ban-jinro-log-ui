query listVillages($input: VillagesInput!) {
  villages(input: $input) {
    totalItems
    items {
      id
      number
      endDate
      url
      name
      people
      cast
      bans {
        position
        trip
      }
    }
  }
}
